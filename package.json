{
  "name": "image-segmentation-karger",
  "version": "1.0.0",
  "description": "Image Foreground Extraction using Karger's Min-Cut Algorithm",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "dev:client": "cd client && npm run dev",
    "dev:server": "cd server && npm run dev",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build"
  },
  "keywords": ["karger", "min-cut", "image-segmentation", "graph-algorithms"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
